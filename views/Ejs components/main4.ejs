<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Benny Estimates - CTA Section</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet"/>
    <script>
        tailwind.config = {
            darkMode: 'class',
            theme: {
                extend: {
                    colors: {
                        primary: {
                            50: '#eff6ff',
                            100: '#dbeafe',
                            200: '#bfdbfe',
                            300: '#93c5fd',
                            400: '#60a5fa',
                            500: '#3b82f6',
                            600: '#2563eb',
                            700: '#1d4ed8',
                            800: '#1e40af',
                            900: '#1e3a8a',
                        },
                        accent: {
                            red: '#ef4444',
                            orange: '#f97316',
                            amber: '#f59e0b',
                        }
                    },
                    animation: {
                        'fade-in': 'fadeIn 0.8s ease-in-out',
                        'pulse-slow': 'pulse 2s ease-in-out infinite',
                        'bounce-slow': 'bounce 2s infinite',
                        'float': 'float 3s ease-in-out infinite',
                    },
                    keyframes: {
                        fadeIn: {
                            '0%': { opacity: '0', transform: 'translateY(20px)' },
                            '100%': { opacity: '1', transform: 'translateY(0)' },
                        },
                        float: {
                            '0%, 100%': { transform: 'translateY(0)' },
                            '50%': { transform: 'translateY(-10px)' },
                        }
                    }
                }
            }
        }
    </script>
    <style>
        /* Theme transition */
        * {
            transition: background-color 0.3s ease, color 0.3s ease, border-color 0.3s ease, transform 0.3s ease;
        }
        
        /* Background overlay for better text readability */
        .bg-overlay {
            position: relative;
        }
        
        .bg-overlay::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, rgba(15, 23, 42, 0.85) 0%, rgba(30, 41, 59, 0.9) 100%);
            z-index: 1;
        }
        
        .dark .bg-overlay::before {
            background: linear-gradient(135deg, rgba(0, 0, 0, 0.85) 0%, rgba(15, 23, 42, 0.9) 100%);
        }
        
        /* Content positioning */
        .cta-content {
            position: relative;
            z-index: 2;
        }
        
        /* Button animations */
        .btn-pulse:hover {
            animation: pulse 1s infinite;
        }
        
        /* Icon animations */
        .icon-float {
            animation: float 3s ease-in-out infinite;
        }
        
        /* Decorative element animations */
        .decoration-1 {
            animation: float 4s ease-in-out infinite;
        }
        
        .decoration-2 {
            animation: float 3.5s ease-in-out infinite reverse;
        }
    </style>
</head>
<body class="bg-gray-50 dark:bg-gray-900 text-gray-900 dark:text-gray-100 min-h-screen">

    <!-- CTA Section -->
    <section class="relative overflow-hidden">
        <!-- Background Image with Overlay -->
        <div class="bg-overlay" 
             style="background-image: url('https://images.unsplash.com/photo-1541888946425-d81bb19240f5?q=80&w=1170&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D');
                    background-size: cover;
                    background-position: center;
                    background-attachment: fixed;">
            
            <!-- Decorative Elements -->
            <div class="absolute top-10 left-10 w-20 h-20 bg-yellow-400/20 rounded-full blur-xl decoration-1"></div>
            <div class="absolute bottom-10 right-10 w-32 h-32 bg-blue-400/20 rounded-full blur-xl decoration-2"></div>
            <div class="absolute top-1/2 left-1/4 w-16 h-16 bg-primary-400/20 rounded-full blur-xl decoration-1"></div>
            
            <!-- Parent Container -->
            <div class="container mx-auto px-4 py-16 lg:py-24">
                <div class="cta-content animate-fade-in">
                    <div class="flex flex-col lg:flex-row items-center justify-between gap-8 lg:gap-12">
                        
                        <!-- Child 1: Text Content -->
                        <div class="lg:w-2/3 text-center lg:text-left">
                            <!-- Section Label -->
                            <div class="inline-flex items-center mb-4">
                                <div class="w-12 h-0.5 bg-white/50 mr-3"></div>
                                <span class="text-white/90 font-semibold uppercase tracking-wider text-sm flex items-center">
                                    <i class="ri-rocket-line mr-2"></i>
                                    READY TO TRANSFORM YOUR BUSINESS?
                                </span>
                            </div>

                            <!-- Main Heading -->
                            <h1 class="text-3xl lg:text-4xl xl:text-5xl font-bold text-white mb-6">
                                Let's Build Something 
                                <span class="text-primary-400">Great Together</span>
                            </h1>

                            <!-- Description -->
                            <p class="text-lg lg:text-xl text-white/90 leading-relaxed mb-8 max-w-2xl">
                                Contact Benny Estimates today to get started on your construction projects and experience the difference 
                                of working with industry experts. Our team is ready to help you win more bids 
                                with accurate, reliable estimates.
                            </p>

                            <!-- Features List -->
                            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                                <div class="flex items-center">
                                    <div class="w-10 h-10 bg-white/10 rounded-full flex items-center justify-center mr-3">
                                        <i class="ri-check-line text-primary-400 text-lg"></i>
                                    </div>
                                    <span class="text-white font-medium">98% Accuracy Guarantee</span>
                                </div>
                                <div class="flex items-center">
                                    <div class="w-10 h-10 bg-white/10 rounded-full flex items-center justify-center mr-3">
                                        <i class="ri-time-line text-primary-400 text-lg"></i>
                                    </div>
                                    <span class="text-white font-medium">24-48 Hour Turnaround</span>
                                </div>
                                <div class="flex items-center">
                                    <div class="w-10 h-10 bg-white/10 rounded-full flex items-center justify-center mr-3">
                                        <i class="ri-award-line text-primary-400 text-lg"></i>
                                    </div>
                                    <span class="text-white font-medium">10+ Years Experience</span>
                                </div>
                            </div>

                            <!-- Contact Info -->
                            <div class="flex flex-wrap gap-6 mb-8">
                                <div class="flex items-center">
                                    <div class="w-8 h-8 bg-primary-500 rounded-full flex items-center justify-center mr-3">
                                        <i class="ri-phone-line text-white text-sm"></i>
                                    </div>
                                    <div>
                                        <div class="text-white/80 text-sm">Call us anytime</div>
                                        <div class="text-white font-bold">+1 (289) 801-7224</div>
                                    </div>
                                </div>
                                <div class="flex items-center">
                                    <div class="w-8 h-8 bg-primary-500 rounded-full flex items-center justify-center mr-3">
                                        <i class="ri-mail-line text-white text-sm"></i>
                                    </div>
                                    <div>
                                        <div class="text-white/80 text-sm">Email us at</div>
                                        <div class="text-white font-bold">Ben@bennyestimates.com</div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Child 2: CTA Button Card -->
                        <div class="lg:w-1/3">
                            <div class="bg-gradient-to-br from-primary-500 via-primary-600 to-primary-700 rounded-2xl p-1 shadow-2xl hover:shadow-3xl transition-all duration-500 icon-float">
                                <div class="bg-white dark:bg-gray-900 rounded-2xl p-8 text-center">
                                    <!-- Icon -->
                                    <div class="w-20 h-20 mx-auto bg-gradient-to-br from-primary-500 to-primary-600 rounded-full flex items-center justify-center mb-6">
                                        <i class="ri-tools-fill text-white text-3xl"></i>
                                    </div>
                                    
                                    <!-- Heading - Changed to "Send request" -->
                                    <h3 class="text-2xl font-bold text-gray-900 dark:text-white mb-4">
                                        Send request
                                    </h3>
                                    
                                    <!-- Description -->
                                    <p class="text-gray-600 dark:text-gray-300 mb-6">
                                        Receive a precise, no-obligation estimate for your construction project within 24 hours.
                                    </p>
                                    
                                    <!-- Stats -->
                                    <div class="grid grid-cols-2 gap-4 mb-6">
                                        <div class="bg-gray-100 dark:bg-gray-800 p-3 rounded-lg">
                                            <div class="text-xl font-bold text-primary-600 dark:text-primary-400">8000+</div>
                                            <div class="text-sm text-gray-600 dark:text-gray-400">Projects</div>
                                        </div>
                                        <div class="bg-gray-100 dark:bg-gray-800 p-3 rounded-lg">
                                            <div class="text-xl font-bold text-primary-600 dark:text-primary-400">98%</div>
                                            <div class="text-sm text-gray-600 dark:text-gray-400">Accuracy</div>
                                        </div>
                                    </div>
                                    
                                    <!-- Main CTA Button -->
                                    <a href="mailto:Ben@bennyestimates.com" class="block w-full bg-gradient-to-r from-primary-600 to-primary-700 text-white font-bold py-4 px-6 rounded-xl hover:shadow-xl transition-all duration-300 transform hover:-translate-y-1 btn-pulse mb-4">
                                        <div class="flex items-center justify-center">
                                            <i class="ri-mail-send-line mr-2 text-lg"></i>
                                            Send request
                                        </div>
                                        <div class="text-xs text-white/80 mt-1">Ben@bennyestimates.com</div>
                                    </a>
                                    
                                    <!-- Secondary Info -->
                                    <div class="text-sm text-gray-500 dark:text-gray-400 mt-4">
                                        <i class="ri-shield-check-line text-green-500 mr-2"></i>
                                        No obligation • Free consultation • 100% confidential
                                    </div>
                                </div>
                            </div>
                            
                            <!-- Additional Info Card - REMOVED -->
                        </div>
                    </div>
                    
                    <!-- Bottom Row: Additional CTAs - REMOVED -->
                    
                    <!-- Trust Badge -->
                    <div class="text-center mt-8">
                        <p class="text-white/60 text-sm">
                            Join <span class="font-bold text-white">700+ construction professionals</span> who trust Benny Estimates for accurate project estimates
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
        // Theme functionality (same as your existing system)
        const themeToggle = document.getElementById('themeToggle');
        const themeIcon = document.getElementById('themeIcon');
        const themeText = document.getElementById('themeText');
        
        // Check for saved theme preference or use default
        const savedTheme = localStorage.getItem('theme');
        const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
        const currentTheme = savedTheme || (prefersDark ? 'dark' : 'light');
        
        // Apply theme on page load
        if (currentTheme === 'dark') {
            document.documentElement.classList.add('dark');
            if (updateThemeUI) updateThemeUI('dark');
        } else {
            document.documentElement.classList.remove('dark');
            if (updateThemeUI) updateThemeUI('light');
        }
        
        // Update theme UI elements
        function updateThemeUI(theme) {
            if (theme === 'dark') {
                if (themeIcon) themeIcon.className = 'ri-sun-line mr-2';
                if (themeText) themeText.textContent = 'Light Mode';
            } else {
                if (themeIcon) themeIcon.className = 'ri-moon-line mr-2';
                if (themeText) themeText.textContent = 'Dark Mode';
            }
        }
        
        // Toggle theme function
        function toggleTheme() {
            if (document.documentElement.classList.contains('dark')) {
                document.documentElement.classList.remove('dark');
                localStorage.setItem('theme', 'light');
                updateThemeUI('light');
            } else {
                document.documentElement.classList.add('dark');
                localStorage.setItem('theme', 'dark');
                updateThemeUI('dark');
            }
        }
        
        // Add event listener for theme toggle if exists
        if (themeToggle) {
            themeToggle.addEventListener('click', toggleTheme);
        }
        
        // Listen for system theme changes
        window.matchMedia('(prefers-color-scheme: dark)').addEventListener('change', (e) => {
            if (!localStorage.getItem('theme')) {
                if (e.matches) {
                    document.documentElement.classList.add('dark');
                    updateThemeUI('dark');
                } else {
                    document.documentElement.classList.remove('dark');
                    updateThemeUI('light');
                }
            }
        });
    </script>
</body>
</html>